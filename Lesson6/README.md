# Lesson 6 Async function

add promises
add async await
add try-catch

in order to work with this project you have to install packages
```
npm i
```

and you can run the code files using node, e.g.
```
node ./src/setters-getters.js
```